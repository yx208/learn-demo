<!--components/navi/index.wxml-->
<view class="container">
    <image bind:tap="onLeft" class="icon" src="{{ latest ? disPrevSrc : prevSrc }}" />
    <text class="title">{{ title }}</text>
    <image bind:tap="onRight" class="icon" src="{{ first ? disNextSrc : nextSrc }}" />
</view>
