const getters = {
	shelfBooks: 					state => state.shelfBooks,
	isEditStatus:					state => state.isEditStatus,
	countCompleteBooks: 	state => state.countCompleteBooks,
	bookMaxHeight:				state => state.bookMaxHeight,
	selectBooks:					state => state.selectBooks
}

export default getters
